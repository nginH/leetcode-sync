# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2100379625 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-09T13:19:00.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

private:
    int ans=0;
    void dfs(TreeNode *root, int rootValue){
        if(!root){
            return ;
        }

        if(root->val >=rootValue){
            ans++;
        }

        dfs(root->left, rootValue);
        dfs(root->right, rootValue);
    }

public:
    int goodNodes(TreeNode* root) {

        if(!root){
            return 0;
        }

        dfs(root, root->val);

        return ans;
    }
};
```

## Test Results

- **Correct:** 11
- **Total testcases:** 63

## Last Testcase

```text
[2,null,4,10,8,null,null,4]
```

## Code Output

```text
5
```

## Expected Output

```text
4
```

