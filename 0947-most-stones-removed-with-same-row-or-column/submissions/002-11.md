# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2139976858 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-09-12T20:02:31.000Z |

---

## Code

```cpp

class DSU {
    vector<int> parent, size;
    public:
    DSU(int n ){
        parent.resize(n);
        size.resize(n, 1);
        iota(parent.begin(), parent.end(), 0);
    }

    int find(int n){
        if(parent[n]==n){
            return n;
        }
        return parent[n]=find(parent[n]);
    }
    bool unite(int a, int b){
        a=find(a);
        b=find(b);
        if(a==b){
            return true;
        }

        if(size[a]>size[b]){
            parent[b]=a;
            size[a]+=size[b];
        }else{
            parent[a]=b;
            size[b]+=size[a];
        }
        return false;
    }

};



class Solution {
public:
    int removeStones(vector<vector<int>>& stones) {
        const int OFFSET = 10001;
        int n=stones.size();
        DSU dsu(20002);
        for(auto x:stones){
            dsu.unite(x[0], x[1]+OFFSET);
        }

        unordered_set<int>st; // parent list..

        for(auto x:stones){
            st.insert(dsu.find(x[0]));
            st.insert(dsu.find(x[1]));   
        };
        // 2 group.2 parents  1 parents . 
        return stones.size()-st.size(); // total nodes - total groups .. 

    }
};
```

## Test Results

- **Correct:** 16
- **Total testcases:** 69

## Last Testcase

```text
[[1,2],[1,3],[3,3],[3,1],[2,1],[1,0]]
```

## Code Output

```text
4
```

## Expected Output

```text
5
```

