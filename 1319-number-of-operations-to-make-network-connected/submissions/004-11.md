# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2138991781 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-09-11T22:05:26.000Z |

---

## Code

```cpp
class DSU{
    vector<int>parent, size;
    public:
        DSU(int n){
            parent.resize(n);
            size.resize(n, 1);
            iota(parent.begin(), parent.end(), 0);
        }

        int find(int n){
            if(parent[n]==n){
                return n;
            }
            return parent[n]=find(parent[n]);
        }

        bool unite(int n,int m){
            int u=find(n);
            int v=find(m);
            if(u==v){
                return true;
            }
            if(size[u]>size[v]){
                parent[v]=u;
                size[u]+=size[v];
            }else{
                parent[u]=v;
                size[v]+=size[u];
            }
            return false;
        }
};


class Solution {
public:
    int makeConnected(int n, vector<vector<int>>& connections) {
        if(connections.size()+1!=n){ //more cable is okay 
            return -1; // less cable return -1
        }
        DSU dsu(n);
        int ans=0;
        for(auto x:connections){
            if(dsu.unite(x[0], x[1])){
                ans++;
            }
        }
        return ans;
    }
};
```

## Test Results

- **Correct:** 5
- **Total testcases:** 36

## Last Testcase

```text
12
[[1,5],[1,7],[1,2],[1,4],[3,7],[4,7],[3,5],[0,6],[0,1],[0,4],[2,6],[0,3],[0,2]]
```

## Code Output

```text
-1
```

## Expected Output

```text
4
```

