# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2048335120 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 77.1 MB |
| Memory Display | 77.1 MB |
| Memory Percentile | 46.04840000000007% |
| Submitted | 2026-06-28T01:20:56.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:

    ListNode* reverse(ListNode*head){
        ListNode*curr=head, *prev=nullptr, *next=nullptr;
        while(curr){
            next=curr->next;
            curr->next=prev;
            prev=curr;
            curr=next;
        }

        return prev;
    }

    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {


        int carry=0;
        
        ListNode *dummy=nullptr;
        while(l1 or l2 or carry){
            int val= (l1 ? l1->val:0) + 
                     (l2 ? l2->val:0) +
                     carry;

            carry=val/10; // carry
            val%=10;
            ListNode*temp= new ListNode(val);
      

            temp->next=dummy;
            dummy=temp;
           

            if(l1){
                l1=l1->next;
            }
            if(l2){
                l2=l2->next;
            }
        }

        return reverse(dummy);
    }
};
```

## Test Results

- **Correct:** 1569
- **Total testcases:** 1569

