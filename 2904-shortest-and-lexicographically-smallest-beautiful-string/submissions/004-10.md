# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2121274048 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 1 ms ms |
| Runtime Display | 1 ms |
| Runtime Percentile | 43.2937% |
| Memory | 8.7 MB |
| Memory Display | 8.7 MB |
| Memory Percentile | 86.7405% |
| Submitted | 2026-08-26T20:30:19.000Z |

---

## Code

```cpp
class Solution {
public:
    string shortestBeautifulSubstring(string s, int k) {
        string ans = ""; // inf
        int left = 0;
        int ones=0;
        for (int right = 0; right < s.size(); right++) {

            if (s[right] == '1') {
                ones++;   
            }

            if (k == ones) {
                while (left < s.size() && s[left] != '1') {
                    left++;
                };
                string temp = s.substr(left, right - left + 1);
                if (ans.empty() ||
                    temp.size() < ans.size() ||
                    (temp.size() == ans.size() && temp < ans)) {
                    ans = temp;
                }

                left++; // increment 1 show that it can use next
                ones--;
            }
        }
        return ans.size() == s.size() + 1 ? "" : ans;
    }
};
```

## Test Results

- **Correct:** 674
- **Total testcases:** 674

