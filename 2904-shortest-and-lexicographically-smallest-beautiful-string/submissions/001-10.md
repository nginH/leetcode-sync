# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2121275821 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 8.8 MB |
| Memory Display | 8.8 MB |
| Memory Percentile | 48.063199999999995% |
| Submitted | 2026-08-26T20:33:48.000Z |

---

## Code

```cpp
class Solution {
public:
    string shortestBeautifulSubstring(string s, int k) {
        string ans = s+'-'; // inf
        int left = 0;
        for (int right = 0; right < s.size(); right++) {

            if (s[right] == '1') {
                k--;   
            }

            if (k == 0) {
                while (left < s.size() && s[left] != '1') {
                    left++;
                };
                string temp = s.substr(left, right - left + 1);
                ans = ((ans.size() > temp.size()) || (ans.size() == temp.size() && temp<ans  )) ? temp : ans;

                left++; // increment 1 show that it can use next
                k++;
            }
        }
        return ans.size() == s.size() + 1 ? "" : ans;
    }
};
```

## Test Results

- **Correct:** 674
- **Total testcases:** 674

