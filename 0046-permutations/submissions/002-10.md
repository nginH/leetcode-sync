# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2104143181 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 3 ms ms |
| Runtime Display | 3 ms |
| Runtime Percentile | 25.943200000000004% |
| Memory | 10.8 MB |
| Memory Display | 10.8 MB |
| Memory Percentile | 40.78039999999999% |
| Submitted | 2026-08-12T12:13:25.000Z |

---

## Code

```cpp
class Solution {

 vector<vector<int>>  ans;

void solve(vector<int>&nums, int i, int j ){
    if(i>=nums.size()){
        ans.push_back(nums);
        return;
    }
    if(j>=nums.size()){ // j out of bound .
        return;
    }

    // take ...
    swap(nums[i], nums[j]);
    solve(nums, i+1, i+1);
    swap(nums[i], nums[j]);

    solve(nums, i, j+1); // skip


}


public:
    vector<vector<int>> permute(vector<int>& nums) {
        solve(nums, 0, 0);
        return ans;
    }
};
```

## Test Results

- **Correct:** 26
- **Total testcases:** 26

