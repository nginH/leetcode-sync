# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2106488693 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-14T10:32:50.000Z |

---

## Code

```cpp
class Solution {
public:
    vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {
        int n= prerequisites.size();
        vector<vector<int>>adj(numCourses);
        vector<int>indegree(numCourses);

        for(auto x:prerequisites){
            adj[x[1]].push_back(x[0]);
            indegree[x[0]]++;
        };
        queue<int>q;

        for(int i=0; i<indegree.size(); i++){
            if(indegree[i]==0){
                q.push(i);
            }
        };
        vector<int> ans;
    
        while(!q.empty()){
            int sz=q.size();
            // first iteration
            while(sz--){
                int curr=q.front();
                ans.push_back(curr);
                q.pop();
                for(auto x:adj[curr]){
                    indegree[x]--;
                    if(indegree[x]==0){
                        q.push(x);
                    }
                }
            }
        }
        return ans;
    }
};
```

## Test Results

- **Correct:** 35
- **Total testcases:** 47

## Last Testcase

```text
3
[[1,0],[1,2],[0,1]]
```

## Code Output

```text
[2]
```

## Expected Output

```text
[]
```

