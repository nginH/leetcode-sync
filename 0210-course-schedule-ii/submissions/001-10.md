# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2106489736 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 5 ms ms |
| Runtime Display | 5 ms |
| Runtime Percentile | 44.285% |
| Memory | 18.9 MB |
| Memory Display | 18.9 MB |
| Memory Percentile | 29.235500000000002% |
| Submitted | 2026-08-14T10:34:01.000Z |

---

## Code

```cpp
class Solution {
public:
    vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {
        int n= prerequisites.size();
        vector<vector<int>>adj(numCourses);
        vector<int>indegree(numCourses);

        for(auto x:prerequisites){
            adj[x[1]].push_back(x[0]);
            indegree[x[0]]++;
        };
        queue<int>q;

        for(int i=0; i<numCourses; i++){
            if(indegree[i]==0){
                q.push(i);
            }
        };
        vector<int> ans;
    
        while(!q.empty()){
            int sz=q.size();
            // first iteration
            while(sz--){
                int curr=q.front();
                ans.push_back(curr);
                q.pop();
                for(auto x:adj[curr]){
                    indegree[x]--;
                    if(indegree[x]==0){
                        q.push(x);
                    }
                }
            }
        }
        if(ans.size()!=numCourses){
            return {};
        }
        return ans;
    }
};
```

## Test Results

- **Correct:** 47
- **Total testcases:** 47

