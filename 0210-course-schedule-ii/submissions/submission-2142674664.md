# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2142674664 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 5 ms ms |
| Runtime Display | 5 ms |
| Runtime Percentile | 44.3851% |
| Memory | 19.1 MB |
| Memory Display | 19.1 MB |
| Memory Percentile | 21.362900000000025% |
| Submitted | 2026-09-15T14:17:29.000Z |

---

## Code

```cpp
class Solution {
public:
    vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {
        vector<vector<int>> adj(numCourses);
        vector<int> indegree(numCourses);

        for (auto x : prerequisites) {
            indegree[x[0]]++;
            adj[x[1]].push_back(x[0]);
        }

        queue<int> q;
        vector<int> ans;
        for (int i = 0; i < indegree.size(); i++) {
            if (indegree[i] == 0) {
                q.push(i);
            }
        }

        while (!q.empty()) {
            int top = q.front();
            ans.push_back(top);
            q.pop();
            for (auto x : adj[top]) {
                indegree[x]--;
                if (indegree[x] == 0) {
                    q.push(x);
                }
            }
        }
        if (ans.size() == numCourses)
            return ans;
        else
            return {};
    }
};
```

## Test Results

- **Correct:** 47
- **Total testcases:** 47

