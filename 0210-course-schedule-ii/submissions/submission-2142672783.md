# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2142672783 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-09-15T14:15:42.000Z |

---

## Code

```cpp
class Solution {
public:
    vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {
        vector<vector<int>>adj(numCourses);
        vector<int>indegree(numCourses);

        for(auto x:prerequisites){
            indegree[x[0]]++;
            adj[x[1]].push_back(x[0]);
        }

        queue<int>q;
        vector<int>ans;
        for(int i=0; i<indegree.size(); i++){
            if(indegree[i]==0){
                q.push(i);
                break;
            }
        }

        while(!q.empty()){
            int top=q.front();
            ans.push_back(top);
            q.pop();
            for(auto x:adj[top]){
                indegree[x]--;
                if(indegree[x]==0){
                    q.push(x);
                }
            }    
        }
        vector<int>_t;
        return ans.size()==numCourses?ans:_t;
        
    }
};
```

## Test Results

- **Correct:** 35
- **Total testcases:** 47

## Last Testcase

```text
2
[]
```

## Code Output

```text
[]
```

## Expected Output

```text
[1,0]
```

