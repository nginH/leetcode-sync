# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2122861843 |
| Status | 20 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-28T12:25:04.000Z |

---

## Code

```cpp
class Solution {
public:
    long long weightedSum(vector<int>& parent, vector<int>& nums) {
        int n=parent.size();
        vector<vector<int>>adj(n);

        for(int i=1; i<parent.size(); i++){
            adj[parent[i]].push_back(i); 
        }
        int h=adj.size();
        /*
         which gives 0=[5]
            1->[1, 2, 3] // level one 
            2->[4, 6]
         */

        queue<int>q;
        q.push(0);

        long long ans=0;
        long long sum=0;
        /*
         E(nums[i])+h-d+1
         E(nums[i])h-E(nums[d])+E(nums[i])

         E(nums[i])*h
         */
        for (int x : nums) // E(nums[i])*h
            sum += x;

        int d=0;
         while(!q.empty()){
            int sz=q.size();
            d++;
            while(sz--){
                auto node =q.front();
                q.pop();

                ans+=1LL*nums[node]*(1-d);//  -E(nums[d])+E(nums[i])

                for(auto &kid:adj[node]){
                    q.push(kid);
                }
            }
         }
         long long kkk=1LLsum*d ;// E(nums[i])h 
          ans=ans+ kkk; // E(nums[i])h -E(nums[d])+E(nums[i])
        return ans;
    }
};
```

## Test Results

- **Total testcases:** 0

## Compile Error

```text
Line 46: Char 25: error: invalid suffix 'LLsum' on integer constant
   46 |          long long kkk=1LLsum*d ;// E(nums[i])h
      |                         ^
1 error generated.
```

## Code Output

```text
{"compile_error": "Line 46: Char 25: error: invalid suffix 'LLsum' on integer constant\n   46 |          long long kkk=1LLsum*d ;// E(nums[i])h\n      |                         ^\n1 error generated."}
```

