# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2117687080 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-23T18:06:10.000Z |

---

## Code

```cpp
class Solution {
public:
    long long maximumSubarraySum(vector<int>& nums, int k) {
        
        // widow sliding ..
        unordered_set<int>st;

        int left=0;
        int sum=0;
        for(int i=0; i<k; i++){ 
            st.insert(nums[i]);
            sum+=nums[i];
        }
       int  ans=0;
        for(int right=k; right<nums.size(); right++){
            bool isValid=true;

            if(st.contains(nums[right])){
                isValid=false;
            }
            sum-=nums[left];
            sum+=nums[right];

            st.erase(nums[left]); // remove left 
            st.insert(nums[right]); // add right

            left++;
            if(isValid){
                ans=max(ans, sum);
            }


        }

        return ans;



    }
};
```

## Test Results

- **Correct:** 37
- **Total testcases:** 93

## Last Testcase

```text
[1,2,2]
2
```

## Code Output

```text
0
```

## Expected Output

```text
3
```

