# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2115088433 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 11.8 MB |
| Memory Display | 11.8 MB |
| Memory Percentile | 67.87259999999998% |
| Submitted | 2026-08-21T13:33:20.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
int ans=0;
void dfs(TreeNode* root, int curr){
    if(!root){
        return;
    }
    curr=curr*10+root->val;
    
    if(!root->left && !root->right){
        ans+=curr;
    }
    dfs(root->left, curr);
    dfs(root->right, curr);

}


public:
    int sumNumbers(TreeNode* root) {
        
        dfs(root, 0);
        return ans;
    }   
};
```

## Test Results

- **Correct:** 108
- **Total testcases:** 108

