# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2067065423 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 11.9 MB |
| Memory Display | 11.9 MB |
| Memory Percentile | 67.87259999999998% |
| Submitted | 2026-07-14T07:35:38.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

private:

    int dfs(TreeNode * root, int ans){
        if(!root){
            return 0;
        }

        ans=ans*10+root->val;
        if(!root->left && !root->right){
            return ans;
        }

        return dfs(root->left, ans)+dfs(root->right, ans);

    }


public:
    int sumNumbers(TreeNode* root) {
        return dfs(root,0);
    }
};
```

## Test Results

- **Correct:** 108
- **Total testcases:** 108

