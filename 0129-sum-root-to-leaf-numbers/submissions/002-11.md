# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2115085881 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-21T13:30:29.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
int ans=0;
void dfs(TreeNode* root, int curr){
    if(!root){
        ans+=curr;
        return;
    }
    curr=curr*10+root->val;
    dfs(root->left, curr);
    dfs(root->right, curr);

}


public:
    int sumNumbers(TreeNode* root) {
        
        dfs(root, 0);
        return ans/2;
    }   
};
```

## Test Results

- **Correct:** 9
- **Total testcases:** 108

## Last Testcase

```text
[4,9,0,null,1]
```

## Code Output

```text
555
```

## Expected Output

```text
531
```

