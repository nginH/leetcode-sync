# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2109039920 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 20.3 MB |
| Memory Display | 20.3 MB |
| Memory Percentile | 12.066500000000003% |
| Submitted | 2026-08-16T13:45:07.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* removeElements(ListNode* head, int val) {
        
        // remvoe
        
        ListNode* prev=nullptr;
        ListNode* curr=head;
        while(curr){

            if(curr->val==val){
                ///delete

                if(prev==nullptr){//head;
                    head=curr->next;
                    delete curr;
                    curr=head;
                }else{
                    prev->next=curr->next;
                    delete curr;
                    curr=prev;
                }

            }else{
                prev=curr;
                curr=curr->next;
            }
           
        }

        return head;
    }
};
```

## Test Results

- **Correct:** 66
- **Total testcases:** 66

