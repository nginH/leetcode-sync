# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2112889341 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 19 ms ms |
| Runtime Display | 19 ms |
| Runtime Percentile | 6.102400000000008% |
| Memory | 13.4 MB |
| Memory Display | 13.4 MB |
| Memory Percentile | 10.1892% |
| Submitted | 2026-08-19T15:46:18.000Z |

---

## Code

```cpp
class Solution {
public:
    int maxVowels(string s, int k) {
        unordered_set<char>st={'a', 'e','i','o','u'};
        
        int ans=0;
        int maxAns=0;
        for(int i=0; i<k; i++){
            if(st.contains(s[i])){
                ans++;
            }
        }
        maxAns=ans;
        int left=0;
        for(int right=k; right<s.size(); right++){

            if(st.contains(s[right])){
                ans++;
            };
            if(st.contains(s[left])){
                ans--;
            }

            left++;
            maxAns=max(maxAns, ans);

            if(maxAns==k){
                return maxAns;// all volel .. no further need to check
            }
        }
    
        return maxAns;



    }
};
```

## Test Results

- **Correct:** 107
- **Total testcases:** 107

