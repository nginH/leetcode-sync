# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2099085270 |
| Status | 15 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-08T12:30:37.000Z |

---

## Code

```cpp
class Solution {
public:
    double findMedianSortedArrays(vector<int>& n1, vector<int>& n2) {
        if(n1.size()<n2.size()){
            return findMedianSortedArrays(n2,n1);
        }
        int m=n1.size();
        int n=n2.size();

        int left=0;
        int right=m;

        int half= (m+n+1)/2;
        
        while(left<=right){

            int i= left+ (right-left)/2;
            int j=half-i;

            int n1left=(i==0) ?INT_MIN:n1[i-1];
            int n1right=(i==m)?INT_MAX:n1[i];

            int n2left=(j==0)?INT_MIN:n2[j-1];
            int n2right=(j==n)?INT_MAX:n2[j];

            if(n1left<=n2right && n2left<=n1right){
                //odd 
                if((m+n)%2==1){
                    return max(n1left, n2left);
                }

                //even
                int leftMax=max(n1left, n2left);
                int rmax=max(n1right, n2right);
                return (leftMax+rmax)/2.0;
            }
            if(n1left>n2right){
                right=i-1;
            }else{
                left=i+1;
            }
        }
        return 0.0;
    }
};
```

## Test Results

- **Correct:** 1
- **Total testcases:** 2099

## Runtime Error

```text
Line 35: Char 32: runtime error: signed integer overflow: 2 + 2147483647 cannot be represented in type 'int' (solution.cpp)
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior prog_joined.cpp:44:32
```

## Last Testcase

```text
[1,2]
[3,4]
```

## Expected Output

```text
2.50000
```

