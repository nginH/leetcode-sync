# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2101442483 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 3 ms ms |
| Runtime Display | 3 ms |
| Runtime Percentile | 60.9413% |
| Memory | 23 MB |
| Memory Display | 23 MB |
| Memory Percentile | 56.459199999999974% |
| Submitted | 2026-08-10T11:08:10.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

TreeNode* sol(vector<int>& nums,int low ,int high){
    if(low>high){
        return nullptr;
    }
    int mid=(low+high)/2;

    TreeNode*root=new TreeNode(nums[mid]);
    root->left=sol(nums, low, mid-1);
    root->right=sol(nums, mid+1, high);
    return root;

}


public:
    TreeNode* sortedArrayToBST(vector<int>& nums) {
        return sol(nums, 0 , nums.size()-1);
    }
};
```

## Test Results

- **Correct:** 31
- **Total testcases:** 31

