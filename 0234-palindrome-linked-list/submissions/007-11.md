# Attempt 7

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 1782782630 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2025-09-25T23:43:24.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    bool isPalindrome(ListNode* head) {
        int num=0;
        int i=0;
        while(head!=NULL){
            num+= 10*i+head->val;

            head=head->next;
            i++;
        }

        string s= to_string(num);

        string r= s;
        reverse(r.begin(), r.end());

        if(s==r){
            return true;
        }else{
            return false;
        }

    }
};
```

## Test Results

- **Correct:** 47
- **Total testcases:** 93

## Last Testcase

```text
[0,0]
```

## Code Output

```text
false
```

## Expected Output

```text
true
```

