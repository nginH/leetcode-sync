# Attempt 5

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 1782784923 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2025-09-25T23:50:00.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    bool isPalindrome(ListNode* head) {
        int num=0;
        int i=0;
        while(head!=NULL){
            if(head->val!=0){
                num+= 10*i +head->val;
            }

            head=head->next;
            i++;
        }

        if(num==0){
            return true;
        }

        string s= to_string(num);

        string r= s;
        reverse(r.begin(), r.end());



        if(s==r){
            return true;
        }else{
            return false;
        }

    }
};
```

## Test Results

- **Correct:** 49
- **Total testcases:** 93

## Last Testcase

```text
[1,0,0]
```

## Code Output

```text
true
```

## Expected Output

```text
false
```

