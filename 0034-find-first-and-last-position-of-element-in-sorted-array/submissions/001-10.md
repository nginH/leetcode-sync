# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2098145070 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 17.6 MB |
| Memory Display | 17.6 MB |
| Memory Percentile | 17.828300000000006% |
| Submitted | 2026-08-07T15:52:18.000Z |

---

## Code

```cpp
class Solution {
public:

    int first(vector<int>& nums, int target) {

        int low = 0, high = nums.size() - 1;
        int ans = -1;

        while (low <= high) {

            int mid = low + (high - low) / 2;

            if (nums[mid] == target) {
                ans = mid;          // Found one
                high = mid - 1;     // Look for an earlier one
            }
            else if (nums[mid] < target) {
                low = mid + 1;
            }
            else {
                high = mid - 1;
            }
        }

        return ans;
    }

    int last(vector<int>& nums, int target) {

        int low = 0, high = nums.size() - 1;
        int ans = -1;

        while (low <= high) {

            int mid = low + (high - low) / 2;

            if (nums[mid] == target) {
                ans = mid;          // Found one
                low = mid + 1;      // Look for a later one
            }
            else if (nums[mid] < target) {
                low = mid + 1;
            }
            else {
                high = mid - 1;
            }
        }

        return ans;
    }

    vector<int> searchRange(vector<int>& nums, int target) {
        return {first(nums, target), last(nums, target)};
    }
};
```

## Test Results

- **Correct:** 88
- **Total testcases:** 88

