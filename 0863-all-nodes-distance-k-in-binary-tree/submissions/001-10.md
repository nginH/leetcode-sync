# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2100711472 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 7 ms ms |
| Runtime Display | 7 ms |
| Runtime Percentile | 28.572499999999998% |
| Memory | 15.6 MB |
| Memory Display | 15.6 MB |
| Memory Percentile | 23.938199999999988% |
| Submitted | 2026-08-09T18:09:39.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    vector<int> distanceK(TreeNode* root, TreeNode* target, int k) {
        unordered_map<int , vector<int>> adj;

        queue<TreeNode*> q;
        q.push(root);

        while(!q.empty()){
            int sz=q.size();
            while(sz--){
                auto x= q.front();
                q.pop();
                if(x->left){
                    adj[x->val].push_back(x->left->val);
                    adj[x->left->val].push_back(x->val);
                    q.push(x->left);
                }
                if(x->right){
                    adj[x->val].push_back(x->right->val);
                    adj[x->right->val].push_back(x->val);
                    q.push(x->right); 
                }
            }
        }
        vector<int>ans;

        unordered_set<int>st;
        
        queue<int>qq;
        
        
        qq.push(target->val);
        st.insert(target->val);
        
        int i=0;
        while(!qq.empty()){ 
            
            if(i==k){
                while(!qq.empty()){
                    ans.push_back(qq.front());
                    qq.pop();
                }
                return ans;
            }


            int sz= qq.size();
            while(sz--){
                auto x=qq.front();
                qq.pop();
                for(auto node:adj[x]){
                    if(!st.contains(node)){
                        st.insert(node); // mark visited ..
                        qq.push(node); // for next iteration
                    }
                }

            }
            i++;
        }
        return ans;
    }
};  
```

## Test Results

- **Correct:** 57
- **Total testcases:** 57

