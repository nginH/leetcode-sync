# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2045792883 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 23.6 MB |
| Memory Display | 23.6 MB |
| Memory Percentile | 72.53339999999999% |
| Submitted | 2026-06-25T12:44:37.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

private:
int ans=0;
public:

    int maxCalc(TreeNode* node){

        if(!node){
            return 0;
        }
        int a=maxCalc(node->left);
        int b=maxCalc(node->right);
        ans=max(ans, a+b);
        return 1+max(a,b);
    }

    int diameterOfBinaryTree(TreeNode* root) {
        maxCalc(root);

        return ans;
    }
};
```

## Test Results

- **Correct:** 106
- **Total testcases:** 106

