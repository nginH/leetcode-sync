# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2099175365 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 23.7 MB |
| Memory Display | 23.7 MB |
| Memory Percentile | 41.61239999999999% |
| Submitted | 2026-08-08T14:01:22.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

private: 
    int sol(TreeNode* root, int &ans){
        if(!root){
            return 0;
        }

        int lh=sol(root->left, ans);
        int rh=sol(root->right, ans);

        ans=max(ans, lh+rh);
        return 1+max(lh, rh);
    }

public:
    int diameterOfBinaryTree(TreeNode* root) {
        int ans=0;
        sol(root, ans);
        return ans;
    }
};
```

## Test Results

- **Correct:** 106
- **Total testcases:** 106

