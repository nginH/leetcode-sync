# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2061441250 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 43 ms ms |
| Runtime Display | 43 ms |
| Runtime Percentile | 5.055700000000001% |
| Memory | 126.2 MB |
| Memory Display | 126.2 MB |
| Memory Percentile | 5.02789999999999% |
| Submitted | 2026-07-09T07:51:03.000Z |

---

## Code

```cpp
class Solution {
unordered_map<int, int> mp;
int idx;
TreeNode* build(vector<int>post, int l, int r){
    if(l>r){
        return nullptr;
    }

    int rootVal=post[idx--];
    TreeNode *root=new TreeNode(rootVal);
    int mid=mp[rootVal];
    root->right= build(post, mid+1, r);
    root->left=build (post, l, mid-1);

    return root;
}



public:
    TreeNode* buildTree(vector<int>& inorder, vector<int>& postorder) {
        for(int i=0; i<inorder.size(); i++){
            mp[inorder[i]]=i;
        }
        idx = postorder.size() - 1;
        return build(postorder , 0, postorder.size()-1);
    }
};
```

## Test Results

- **Correct:** 202
- **Total testcases:** 202

