# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2087938688 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 73 ms ms |
| Runtime Display | 73 ms |
| Runtime Percentile | 87.80180000000009% |
| Memory | 44.4 MB |
| Memory Display | 44.4 MB |
| Memory Percentile | 70.35270000000007% |
| Submitted | 2026-07-30T18:23:46.000Z |

---

## Code

```cpp
class Solution {
public:
    int networkDelayTime(vector<vector<int>>& times, int n, int k) {
        vector<vector<pair<int, int>>> graph(n + 1);

        for(auto &edge:times){
            int u =edge[0];
            int v =edge[1];
            int w =edge[2];
            graph[u].push_back({v,w});
        }

        vector<int>dist(n+1, INT_MAX);
       priority_queue<pair<int, int>,
                       vector<pair<int, int>>,
                       greater<pair<int, int>>> pq;


        
        dist[k]=0;
        pq.push({0,k});

        while(!pq.empty()){
            auto [d, node]= pq.top();
            pq.pop();
            if(d>dist[node]) continue;

            for(auto &[next, wt]:graph[node]){
                if(dist[next]>d+wt){
                    dist[next]=d+wt;
                    pq.push({dist[next], next});
                }
            }
        }
        int ans=0;

        for(int i=1; i<=n; i++){
            if(dist[i]==INT_MAX){
                return -1;
            }
            ans=max(ans, dist[i]);
        }

        return ans;



    }
};
```

## Test Results

- **Correct:** 56
- **Total testcases:** 56

