# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2097221035 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 27.8 MB |
| Memory Display | 27.8 MB |
| Memory Percentile | 76.5256% |
| Submitted | 2026-08-06T19:41:30.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

int ans=INT_MIN;

    int dfs(TreeNode* root){
       
        if(!root){
            return 0;
        }

        int l=max(0, dfs(root->left)); // if calculation from left is -ve then drop path value as 0
        int r=max(0, dfs(root->right));
        // inclue current node or not ..
        ans= max(ans, l+r+root->val);
        return root->val+ max(l, r);
    }


public:
    int maxPathSum(TreeNode* root) {
        dfs(root);
        return ans;
    }
};
```

## Test Results

- **Correct:** 96
- **Total testcases:** 96

