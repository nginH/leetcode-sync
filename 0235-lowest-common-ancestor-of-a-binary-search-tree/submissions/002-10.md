# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2059938772 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 27 ms ms |
| Runtime Display | 27 ms |
| Runtime Percentile | 22.68009999999998% |
| Memory | 23.5 MB |
| Memory Display | 23.5 MB |
| Memory Percentile | 66.30419999999995% |
| Submitted | 2026-07-08T03:44:53.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */

class Solution {
public:
    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
      if (p->val < root->val && q->val < root->val)
            return lowestCommonAncestor(root->left, p, q);

        if (p->val > root->val && q->val > root->val)
            return lowestCommonAncestor(root->right, p, q);

        return root;

    }
};
```

## Test Results

- **Correct:** 30
- **Total testcases:** 30

