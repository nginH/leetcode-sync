# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2100854455 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 18.9 MB |
| Memory Display | 18.9 MB |
| Memory Percentile | 24.41989999999999% |
| Submitted | 2026-08-09T21:29:12.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {

private:
ListNode* merge2(ListNode* l1, ListNode *l2){
     ListNode dummy(0);
    ListNode* tail = &dummy;

    while(l1 && l2){
        if(l1->val >l2->val){
            tail->next=l2;
            l2=l2->next;
        }else{
            tail->next=l1;
            l1=l1->next;
        }
        tail=tail->next;
    }
    if(l1){
        tail->next=l1;
    }
    if(l2){
        tail->next=l2;
    }
    return dummy.next;
}

void dnq(vector<ListNode*>& lists, int l ,int h){
    if(l<h){
        int mid= (l+h)/2;

        dnq(lists, l, mid);
        dnq(lists, mid+1, h);
       lists[l]= merge2(lists[l], lists[mid+1]);
    }
    
}

public:
    ListNode* mergeKLists(vector<ListNode*>& lists) {  
        if (lists.empty())
            return nullptr;
        dnq(lists, 0, lists.size()-1);
        return lists[0];  
    }
};
```

## Test Results

- **Correct:** 134
- **Total testcases:** 134

