# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2102675100 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 19 ms ms |
| Runtime Display | 19 ms |
| Runtime Percentile | 14.607000000000005% |
| Memory | 18.8 MB |
| Memory Display | 18.8 MB |
| Memory Percentile | 13.003700000000036% |
| Submitted | 2026-08-11T10:13:46.000Z |

---

## Code

```cpp
class Solution {

bool sol(string &s , int idx , unordered_set<string>st, vector<int>&dp){
    if(idx==s.size()){
        return true;
    }
    if(dp[idx]!=-1){
        return dp[idx];
    }

    string curr="";
    for(int i=idx; i<s.size(); i++){
        curr+=s[i];
        if(st.contains(curr)){
           if(sol(s, i+1, st, dp))
                return dp[idx]=1;
        }
    }
    return dp[idx]=false;
}


public:
    bool wordBreak(string s, vector<string>& wordDict) {
        unordered_set<string>st={wordDict.begin(), wordDict.end()};
        vector<int>dp(s.size()+1, -1);

        return sol(s, 0, st , dp);

    }
};
```

## Test Results

- **Correct:** 48
- **Total testcases:** 48

