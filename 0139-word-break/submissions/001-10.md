# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2102697642 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 20 ms ms |
| Runtime Display | 20 ms |
| Runtime Percentile | 11.894700000000004% |
| Memory | 18.8 MB |
| Memory Display | 18.8 MB |
| Memory Percentile | 13.003700000000036% |
| Submitted | 2026-08-11T10:33:33.000Z |

---

## Code

```cpp
class Solution {

bool sol(string &s , int idx , unordered_set<string>st, vector<int>&dp){
    if(idx==s.size()){
        return true;//pura sting pe triverse kiye tabhi to idx end pe paucha .
    }
    if(dp[idx]!=-1){
        return dp[idx];
    }

    string curr="";
    for(int i=idx; i<s.size(); i++){
        curr+=s[i];
        if(st.contains(curr) && (sol(s, i+1, st, dp))) // yeah se increment hua .. and only on sucess
            return dp[idx]=1;
    }
    return dp[idx]=false;
}


public:
    bool wordBreak(string s, vector<string>& wordDict) {
        unordered_set<string>st={wordDict.begin(), wordDict.end()};
        vector<int>dp(s.size()+1, -1);
        return sol(s, 0, st , dp);

    }
};
```

## Test Results

- **Correct:** 48
- **Total testcases:** 48

