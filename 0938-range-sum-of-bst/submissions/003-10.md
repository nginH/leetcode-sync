# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2110028371 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 4 ms ms |
| Runtime Display | 4 ms |
| Runtime Percentile | 7.557500000000001% |
| Memory | 65.1 MB |
| Memory Display | 65.1 MB |
| Memory Percentile | 30.730599999999992% |
| Submitted | 2026-08-17T10:51:04.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {


int ans=0;
void sol(TreeNode* root, int low, int high ){

    if(!root){
        return;
    }

    sol(root->left, low, high);
    if(root->val>=low && root->val<=high){
        ans+=root->val;
    }
    sol(root->right, low, high);
}


public:
    int rangeSumBST(TreeNode* root, int low, int high) {
        sol(root, low, high);
        return ans;
    }
};
```

## Test Results

- **Correct:** 41
- **Total testcases:** 41

