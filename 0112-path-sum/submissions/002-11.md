# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2099111779 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-08T12:58:45.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {



bool dfs(TreeNode *root, int &tar){
    if(!root){
        return false;
    }

    if(!root->left && !root->right){
        return root->val==tar;
    }
    tar-=root->val;
      
    return dfs(root->left, tar)|| dfs(root->right, tar);
    
}


public:
    bool hasPathSum(TreeNode* root, int targetSum) {
       return dfs(root, targetSum);
    }   
};
```

## Test Results

- **Correct:** 100
- **Total testcases:** 118

## Last Testcase

```text
[1,-2,-3,1,3,-2,null,-1]
3
```

## Code Output

```text
true
```

## Expected Output

```text
false
```

