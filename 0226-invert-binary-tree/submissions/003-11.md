# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2099188393 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-08T14:13:29.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

    void dfs(TreeNode*root){
        if(!root){
            return;
        }
        if(root->left && root->right){
            swap(root->left , root->right);
        }
        dfs(root->left) ;
        dfs(root->right);
    }


public:
    TreeNode* invertTree(TreeNode* root) {
        
        dfs(root);
        return root;

    }
};
```

## Test Results

- **Correct:** 8
- **Total testcases:** 77

## Last Testcase

```text
[1,2]
```

## Code Output

```text
[1,2]
```

## Expected Output

```text
[1,null,2]
```

