# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2047345706 |
| Status | 15 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-06-27T02:50:11.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* reverse(ListNode* head) {
        ListNode *curr = head, *next = nullptr, *prev = nullptr;
        while (curr) {
            next = curr->next;
            curr->next = prev;
            prev = curr;
            curr = next;
        }
        return prev;
    }

    void reorderList(ListNode* head) {

        // 1->2->3->4->5
        ListNode *fast = head, *slow = head;

        ListNode* t = nullptr;

        while (fast and fast->next) {
            fast = fast->next->next;
            t = slow;
            slow = slow->next;
        }

        t->next=nullptr;

        ListNode* last = reverse(slow);
        // 1->2->3<-4<-5

        ListNode* temp = head;
        while (last and temp) {
            // presevring the node
            ListNode* a = temp->next;
            ListNode* b = last->next;

            // adjusting the link
            temp->next = last;

            if (!a)
                break;

            last->next = a;

            temp = a;
            last = b;
        }
    }
};
```

## Test Results

- **Correct:** 2
- **Total testcases:** 12

## Runtime Error

```text
Line 37: Char 12: runtime error: member access within null pointer of type 'ListNode' (solution.cpp)
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior prog_joined.cpp:46:12
```

## Last Testcase

```text
[1]
```

## Expected Output

```text
[1]
```

