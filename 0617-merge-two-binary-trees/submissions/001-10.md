# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2101497845 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 33.2 MB |
| Memory Display | 33.2 MB |
| Memory Percentile | 43.1648% |
| Submitted | 2026-08-10T12:07:21.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {




public:
    TreeNode* mergeTrees(TreeNode* root1, TreeNode* root2) {
        if(!root1 && !root2){
            return nullptr;
        }
        if(!root1 ){
            return root2;
        }
        if(!root2){
            return root1;
        }

      

        root1->val+=root2->val;
        root1->left=mergeTrees(root1->left, root2->left);
        root1->right=mergeTrees(root1->right, root2->right);

        return root1;
    }
};
```

## Test Results

- **Correct:** 182
- **Total testcases:** 182

