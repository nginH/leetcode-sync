# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2060182447 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 24.3 MB |
| Memory Display | 24.3 MB |
| Memory Percentile | 91.49559999999995% |
| Submitted | 2026-07-08T07:23:56.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

int ans;
int count=0;
void inord(TreeNode*root, int k){
    if(!root){
        return;
    }

    inord(root->left, k);
    count++;
    if(k==count){
        ans=root->val;
        return;

    }
    inord(root->right, k);
}

public:
    int kthSmallest(TreeNode* root, int k) {
        inord(root,k);
        return ans;


    }
};
```

## Test Results

- **Correct:** 93
- **Total testcases:** 93

