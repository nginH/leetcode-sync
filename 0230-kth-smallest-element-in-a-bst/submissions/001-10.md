# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2097015708 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 24.4 MB |
| Memory Display | 24.4 MB |
| Memory Percentile | 43.18559999999995% |
| Submitted | 2026-08-06T17:04:05.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
private:
    int ans;
    void sol(TreeNode * root, int &k){
        if(!root){
            return;
        }

        sol(root->left, k);
        k--;
        if(k==0){
            ans=root->val;
        }
        sol(root->right, k);
    }


public:
    int kthSmallest(TreeNode* root, int k) {

        sol(root, k);
        return ans;
    }
};
```

## Test Results

- **Correct:** 93
- **Total testcases:** 93

