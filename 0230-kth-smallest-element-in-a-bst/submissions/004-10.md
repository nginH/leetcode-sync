# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2060177385 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 24.5 MB |
| Memory Display | 24.5 MB |
| Memory Percentile | 28.64289999999995% |
| Submitted | 2026-07-08T07:19:07.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

vector<int> arr;
void inord(TreeNode*root){
    if(!root){
        return;
    }
    inord(root->left);
    arr.push_back(root->val);
    inord(root->right);
}

public:
    int kthSmallest(TreeNode* root, int k) {
        inord(root);
        return arr[k-1];


    }
};
```

## Test Results

- **Correct:** 93
- **Total testcases:** 93

