# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2059622641 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-07-07T17:32:05.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

private:
vector<vector<int>> ans;
void dfs(TreeNode *root, int level){

    if(!root){
        return;
    }

    if(level==ans.size()){
        ans.push_back({});
    }

  

    dfs(root->left, level+1);
    dfs(root->right, level+1);
      ans[level].push_back(root->val);

}

public:
    vector<vector<int>> levelOrderBottom(TreeNode* root) {
        dfs(root,0);
        return ans;
    }
};
```

## Test Results

- **Correct:** 2
- **Total testcases:** 34

## Last Testcase

```text
[3,9,20,null,null,15,7]
```

## Code Output

```text
[[3],[9,20],[15,7]]
```

## Expected Output

```text
[[15,7],[9,20],[3]]
```

