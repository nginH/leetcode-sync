# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2117318841 |
| Status | 15 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-23T12:21:25.000Z |

---

## Code

```cpp
class Solution {
public:
    string longestWord(vector<string>& words) {

        sort(words.begin(), words.end());

        unordered_set<string> st;
        priority_queue<pair<int, string>> pq;

        for (auto word : words) {
            string temp = "";
            int countFound = 0;
            bool valid = true;
            for (int i = 0; i < word.size() - 1; i++) {
                temp += word[i];
                if (st.contains(temp)) {
                    countFound++;
                }
                else {
                    valid = false;
                    break;
                }
            }
            st.insert(word);
            if (valid) {
                pq.push({countFound, word});
            }
        }
        vector<string> ans;
        int maxOccur = pq.top().first;
        while (!pq.empty()) {
            int n = pq.top().first;
            if (n == maxOccur) {
                ans.push_back(pq.top().second);
            }
            else {
                break;
            }
            pq.pop();
        }
        sort(ans.begin(), ans.end());
        return ans[0];
    }
};
```

## Test Results

- **Correct:** 58
- **Total testcases:** 59

## Runtime Error

```text
Line 1082: Char 16: runtime error: reference binding to null pointer of type 'const std::pair<int, std::basic_string<char>>' (stl_iterator.h)
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior /usr/lib/gcc/x86_64-linux-gnu/14/../../../../include/c++/14/bits/stl_iterator.h:1091:16
```

## Last Testcase

```text
["wo","wor","worl","world"]
```

## Expected Output

```text
""
```

