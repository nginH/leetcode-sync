# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2053513892 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-07-02T12:39:38.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:

    int length(ListNode *temp){
        int res=0;
        while(temp){
            res+=1;
            temp=temp->next;
        }
        return res;
    }


    ListNode* rotateRight(ListNode* head, int k) {
        int len=length(head);
        
        if(!head || k==0){
            return head;
        }
        k%=len;
        if(k==0){
            return head;
        }
        ListNode *headCopy=head;
        ListNode *temp=head;
        ListNode *prev=nullptr;
        ListNode *last=head;

        while(last->next){
            if(k>=0){
                prev=temp;
                temp=temp->next;
                k--;
            }
            last=last->next; 
        }

        prev->next=nullptr;
        last->next=headCopy;
        return temp;  
    }
};
```

## Test Results

- **Correct:** 45
- **Total testcases:** 232

## Last Testcase

```text
[1,2,3]
2000000000
```

## Code Output

```text
[3,1,2]
```

## Expected Output

```text
[2,3,1]
```

