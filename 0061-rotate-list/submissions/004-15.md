# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2053510620 |
| Status | 15 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-07-02T12:36:00.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:


    ListNode* rotateRight(ListNode* head, int k) {

        if(!head){
            return head;
        }

        ListNode *headCopy=head;
        ListNode *temp=head;
        ListNode *prev=nullptr;
        ListNode *last=head;

        while(last->next){
            if(k>=0){
                prev=temp;
                temp=temp->next;
                k--;
            }
            last=last->next; 
        }

        prev->next=nullptr;
        last->next=headCopy;
        return temp;  
    }
};
```

## Test Results

- **Correct:** 4
- **Total testcases:** 232

## Runtime Error

```text
Line 35: Char 15: runtime error: member access within null pointer of type 'ListNode' (solution.cpp)
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior prog_joined.cpp:44:15
```

## Last Testcase

```text
[1]
0
```

## Expected Output

```text
[1]
```

