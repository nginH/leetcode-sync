# Attempt 6

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2060019734 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-07-08T05:09:47.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool isValidBST(TreeNode* root) {
       if(!root || !root->left || !root->right ){
        return false;
       }

       if(root->left->val<root->val && root->right->val>root->val){
        return true;
       }

       return isValidBST(root->left) && isValidBST(root->right);
    }
};
```

## Test Results

- **Correct:** 47
- **Total testcases:** 86

## Last Testcase

```text
[0]
```

## Code Output

```text
false
```

## Expected Output

```text
true
```

