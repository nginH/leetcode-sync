# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2060162697 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 22 MB |
| Memory Display | 22 MB |
| Memory Percentile | 44.599699999999956% |
| Submitted | 2026-07-08T07:06:04.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {


private: 
    bool dfs(TreeNode *root, int64_t l , int64_t r){

        if(!root){
            return true;
        }

        if(l>=root->val ||r<=root->val){
            return false;
        }

        return dfs(root->left,l , root->val) && dfs(root->right, root->val, r);
    }

public:
    bool isValidBST(TreeNode* root) {
       return dfs(root, LLONG_MIN, LLONG_MAX);
    }
};
```

## Test Results

- **Correct:** 86
- **Total testcases:** 86

