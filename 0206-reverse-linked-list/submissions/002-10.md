# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2056491838 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 13.5 MB |
| Memory Display | 13.5 MB |
| Memory Percentile | 15.773900000000012% |
| Submitted | 2026-07-05T04:07:33.000Z |

---

## Code

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
private:

    ListNode *sol(ListNode *head){
        
        if(!head || !head->next){ // 0 or 1 ..
            return head;
        }

        ListNode *temp=sol(head->next);
        head->next->next=head;
        head->next=nullptr;

        return temp; //
    }


public:
    ListNode* reverseList(ListNode* head) {

        return sol(head);

    }
};
```

## Test Results

- **Correct:** 28
- **Total testcases:** 28

