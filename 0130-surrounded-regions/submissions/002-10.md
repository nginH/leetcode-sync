# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2106535029 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 13.8 MB |
| Memory Display | 13.8 MB |
| Memory Percentile | 89.98729999999999% |
| Submitted | 2026-08-14T11:28:38.000Z |

---

## Code

```cpp
class Solution {

vector<pair<int, int>>dir={
    {1,0},
    {0,1},
    {-1,0},
    {0,-1}
};
int m,n;
void dfs(int row, int col,vector<vector<char>>& board){
    if(row<0|| col<0 || row>=m|| col>=n){
        return;// out of bound;
    }
    if(board[row][col]=='X' || board[row][col]=='#'){
        return;
    }
    board[row][col]='#';
    for(auto &[dx, dy]:dir){
        dfs(row+dx, col+dy, board);
    }
}


public:
    void solve(vector<vector<char>>& board) {
        m=board.size();
        n=board[0].size();
        for(int i=0; i<n; i++){// from row boarder..
            dfs(0,i, board);
            dfs(m-1,i, board);
        }

        for(int i=0; i<m; i++){// from row boarder..
            dfs(i,0, board);
            dfs(i,n-1, board);
        }

        // ---------------------- 

        for(int i=0; i<m; i++){
            for(int j=0; j<n; j++){
                if(board[i][j]=='#'){
                    board[i][j]='O';
                }else if(board[i][j]=='O'){
                    board[i][j]='X';
                }
            }
        }
    }
};
```

## Test Results

- **Correct:** 59
- **Total testcases:** 59

