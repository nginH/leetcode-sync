# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2101997594 |
| Status | 14 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-10T19:25:07.000Z |

---

## Code

```cpp
class Solution {

vector<int>dp;
int sol(vector<int>& coins, int amount, int idx){

    if(amount==0){
        return 0;
    }
    if (amount < 0 || idx >= (int)coins.size()) return INT_MAX;

    int skip=sol(coins, amount, idx+1);
    int take=sol(coins, amount-coins[idx], idx);
    return min(skip, take==INT_MAX?INT_MAX:take+1);
}


public:
    int coinChange(vector<int>& coins, int amount) {
        dp.resize(amount+1, -1);
        int ans= sol(coins, amount,0);
       return ans==INT_MAX?-1:ans;
       
    }
};
```

## Test Results

- **Correct:** 39
- **Total testcases:** 189

## Last Testcase

```text
[411,412,413,414,415,416,417,418,419,420,421,422]
9864
```

## Expected Output

```text
24
```

