# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2059638561 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 15.8 MB |
| Memory Display | 15.8 MB |
| Memory Percentile | 6.791099999999975% |
| Submitted | 2026-07-07T17:44:49.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
 
class Solution {

private:
vector<vector<int>> ans;

    void dfs(TreeNode *root, int level){
    
        if(!root){
            return;
        }

        if(level==ans.size()){
             ans.push_back({});
        }

        if(ans[level].size()==1)
            ans[level][0]=root->val;
        else
            ans[level].push_back(root->val);


        dfs(root->left, level+1);
        dfs(root->right, level+1);

    }

public:
    vector<int> rightSideView(TreeNode* root) {
        dfs(root, 0);
    

        vector<int>k;

        for(auto x:ans){
            k.push_back(x[0]);
        }

        return k;
    }
};
```

## Test Results

- **Correct:** 217
- **Total testcases:** 217

