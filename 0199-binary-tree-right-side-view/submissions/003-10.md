# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2067718649 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 15 MB |
| Memory Display | 15 MB |
| Memory Percentile | 40.655899999999974% |
| Submitted | 2026-07-14T17:47:41.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    vector<int> rightSideView(TreeNode* root) {
        vector<int>ans;
        if(!root){
            return ans;
        }
       queue<TreeNode*>q;
       q.push(root);

       while(!q.empty()){

            int size= q.size();
            int last;

            while(size--){
                TreeNode* t= q.front();
                q.pop();
                last= t->val;
                if(t->left){
                    q.push(t->left);
                }
                if(t->right){
                    q.push(t->right);
                }
            }

            ans.push_back(last);
       }

       return ans;
    }
};
```

## Test Results

- **Correct:** 217
- **Total testcases:** 217

