# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2138807013 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 19.2 MB |
| Memory Display | 19.2 MB |
| Memory Percentile | 97.3868% |
| Submitted | 2026-09-11T17:26:38.000Z |

---

## Code

```cpp
class Solution {

    void dfs(vector<vector<int>>& isConnected, int b, vector<bool>& vis) {

        if (b < 0 || b >= isConnected.size()) {
            return; // out of bound ..
        }

        if (vis[b] == 1) {
            return;
        }
        vis[b] = 1;

        for (int i = 0; i < isConnected.size(); i++) {
            if (isConnected[b][i] == 1) {
                dfs(isConnected, i, vis);
            }
        }
    }

public:
    int findCircleNum(vector<vector<int>>& isConnected) {

        vector<bool> vis(isConnected.size() + 1, false);
        int ans = 0;
        for (int i = 0; i < isConnected.size(); i++) {
            if (!vis[i]) {
                ans++;
                dfs(isConnected, i, vis);
            }
        }

        return ans;
    }
};
```

## Test Results

- **Correct:** 114
- **Total testcases:** 114

