# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2104126123 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 10 MB |
| Memory Display | 10 MB |
| Memory Percentile | 37.495400000000004% |
| Submitted | 2026-08-12T11:53:35.000Z |

---

## Code

```cpp
class Solution {

private:

    void solve(vector<int>& nums, int i, vector<int>&curr,  vector<vector<int>> &ans ){

        if(i==nums.size()){
            ans.push_back(curr);
            return;
        }

        curr.push_back(nums[i]);
        // take .
        solve(nums,i+1, curr, ans);
        curr.pop_back();

        solve(nums, i+1, curr, ans);// skip..
    }


public:
    vector<vector<int>> subsets(vector<int>& nums) {
        

        vector<vector<int>> ans;
        vector<int> curr;

        solve(nums, 0, curr, ans);
        return ans;



    }
};
```

## Test Results

- **Correct:** 10
- **Total testcases:** 10

