# Attempt 5

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2100840233 |
| Status | 20 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-09T20:55:53.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Codec {

string serAns="";
void dfs(TreeNode*root){
    if(!root){
        serAns+='*';
        return;
    }
    serAns+=to_string(root->val)+',';
    dfs(root->left);
    dfs(root->right);
}

int idx=0;
TreeNode* ddfs(vector<string> &s){
    if(s[idx]=="*"){
        idx+=1;
        return nullptr;
    }
    TreeNode*root=new TreeNode(stoi(s[idx]));
    root->left=ddfs(s);
    root->right=ddfs(s);
    return root;

}

public:
    // Encodes a tree to a single string.
    string serialize(TreeNode* root) {
        serAns="";
      dfs(root);
      return serAns;  
    }

    // Decodes your encoded data to tree.
    TreeNode* deserialize(string data) {
        streamstring ss(data);
        vector<string>s;
        string item;
        while(getline(ss, item, ',')){
            if(!item.empty())
                s.push_back(item);
        }
        idx=0;
        return ddfs(s);

    }
};

// Your Codec object will be instantiated and called as such:
// Codec ser, deser;
// TreeNode* ans = deser.deserialize(ser.serialize(root));
```

## Test Results

- **Total testcases:** 0

## Compile Error

```text
Line 46: Char 9: error: unknown type name 'streamstring'; did you mean 'streamsize'?
   46 |         streamstring ss(data);
      |         ^~~~~~~~~~~~
      |         streamsize
/usr/lib/gcc/x86_64-linux-gnu/14/../../../../include/c++/14/bits/postypes.h:68:21: note: 'streamsize' declared here
   63 |   typedef ptrdiff_t     streamsize; // Signed integral type
      |                         ^
```

## Code Output

```text
{"compile_error": "Line 46: Char 9: error: unknown type name 'streamstring'; did you mean 'streamsize'?\n   46 |         streamstring ss(data);\n      |         ^~~~~~~~~~~~\n      |         streamsize\n/usr/lib/gcc/x86_64-linux-gnu/14/../../../../include/c++/14/bits/postypes.h:68:21: note: 'streamsize' declared here\n   63 |   typedef ptrdiff_t     streamsize; // Signed integral type\n      |                         ^"}
```

