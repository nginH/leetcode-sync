# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2100842279 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 23 ms ms |
| Runtime Display | 23 ms |
| Runtime Percentile | 84.30119999999998% |
| Memory | 39.9 MB |
| Memory Display | 39.9 MB |
| Memory Percentile | 20.815099999999976% |
| Submitted | 2026-08-09T21:00:23.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Codec {

string serAns="";
void dfs(TreeNode*root){
    if(!root){
        serAns+="*,";
        return;
    }
    serAns+=to_string(root->val)+',';
    dfs(root->left);
    dfs(root->right);
}

int idx=0;
    TreeNode* ddfs(vector<string>& s) {
        if (s[idx] == "*") {
            idx++;
            return nullptr;
        }

        TreeNode* root = new TreeNode(stoi(s[idx]));
        idx++;

        root->left = ddfs(s);
        root->right = ddfs(s);

        return root;
    }

public:
    // Encodes a tree to a single string.
    string serialize(TreeNode* root) {
        serAns="";
      dfs(root);
      return serAns;  
    }

    // Decodes your encoded data to tree.
    TreeNode* deserialize(string data) {
        stringstream ss(data);
        vector<string>s;
        string item;
        while(getline(ss, item, ',')){
            if(!item.empty())
                s.push_back(item);
        }
        idx=0;
        return ddfs(s);

    }
};

// Your Codec object will be instantiated and called as such:
// Codec ser, deser;
// TreeNode* ans = deser.deserialize(ser.serialize(root));
```

## Test Results

- **Correct:** 53
- **Total testcases:** 53

