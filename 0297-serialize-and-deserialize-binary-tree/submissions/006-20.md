# Attempt 6

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2100840060 |
| Status | 20 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-09T20:55:29.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Codec {

string serAns="";
void dfs(TreeNode*root){
    if(!root){
        serAns+='*';
        return;
    }
    serAns+=to_string(root->val)+',';
    dfs(root->left);
    dfs(root->right);
}

int idx=0;
TreeNode* ddfs(vector<string> &s){
    if(s[idx]=="*"){
        idx+=1;
        return nullptr;
    }
    TreeNode*root=new TreeNode(stoi(s[idx]));
    root->left=dfs(s);
    root->right=dfs(s);
    return root;

}

public:
    // Encodes a tree to a single string.
    string serialize(TreeNode* root) {
        serAns="";
      dfs(root);
      return serAns;  
    }

    // Decodes your encoded data to tree.
    TreeNode* deserialize(string data) {
        streamstring ss(data);
        vector<string>s;
        string item;
        while(getline(ss, item, ',')){
            if(!item.empty())
                s.push_back(item);
        }
        idx=0;
        return ddfs(s);

    }
};

// Your Codec object will be instantiated and called as such:
// Codec ser, deser;
// TreeNode* ans = deser.deserialize(ser.serialize(root));
```

## Test Results

- **Total testcases:** 0

## Compile Error

```text
Line 30: Char 20: error: no viable conversion from 'vector<string>' (aka 'vector<basic_string<char>>') to 'TreeNode *'
   30 |     root->left=dfs(s);
      |                    ^
Line 13: Char 19: note: passing argument to parameter 'root' here
   13 | void dfs(TreeNode*root){
      |                   ^
```

## Code Output

```text
{"compile_error": "Line 30: Char 20: error: no viable conversion from 'vector<string>' (aka 'vector<basic_string<char>>') to 'TreeNode *'\n   30 |     root->left=dfs(s);\n      |                    ^\nLine 13: Char 19: note: passing argument to parameter 'root' here\n   13 | void dfs(TreeNode*root){\n      |                   ^"}
```

