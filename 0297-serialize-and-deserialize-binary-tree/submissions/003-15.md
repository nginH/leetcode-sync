# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2100841089 |
| Status | 15 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-09T20:57:49.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Codec {

string serAns="";
void dfs(TreeNode*root){
    if(!root){
        serAns+='*';
        return;
    }
    serAns+=to_string(root->val)+',';
    dfs(root->left);
    dfs(root->right);
}

int idx=0;
TreeNode* ddfs(vector<string> &s){
    if(s[idx]=="*" || idx>s.size()){
        idx+=1;
        return nullptr;
    }
    TreeNode*root=new TreeNode(stoi(s[idx]));
    idx+=1;
    root->left=ddfs(s);
    root->right=ddfs(s);
    return root;

}

public:
    // Encodes a tree to a single string.
    string serialize(TreeNode* root) {
        serAns="";
      dfs(root);
      return serAns;  
    }

    // Decodes your encoded data to tree.
    TreeNode* deserialize(string data) {
        stringstream ss(data);
        vector<string>s;
        string item;
        while(getline(ss, item, ',')){
            if(!item.empty())
                s.push_back(item);
        }
        idx=0;
        return ddfs(s);

    }
};

// Your Codec object will be instantiated and called as such:
// Codec ser, deser;
// TreeNode* ans = deser.deserialize(ser.serialize(root));
```

## Test Results

- **Correct:** 0
- **Total testcases:** 53

## Runtime Error

```text
terminate called after throwing an instance of 'std::invalid_argument'
  what():  stoi
```

## Last Testcase

```text
[1,2,3,null,null,4,5]
```

## Expected Output

```text
[1,2,3,null,null,4,5]
```

