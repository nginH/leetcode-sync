# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2109406186 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 2 ms ms |
| Runtime Display | 2 ms |
| Runtime Percentile | 10.477799999999995% |
| Memory | 19.4 MB |
| Memory Display | 19.4 MB |
| Memory Percentile | 65.40060000000003% |
| Submitted | 2026-08-16T19:08:51.000Z |

---

## Code

```cpp
class Solution {


int bsearch(vector<int>& numbers, int target, int startIdx){

        int i=startIdx;
        int j=numbers.size()-1;

        while(i<=j){
            int mid=(i+j)/2;
            if(numbers[mid]==target){
                return mid;
            }else if(numbers[mid]<target){// mid is less then target//
                i=mid+1;
            }else{
                j=mid-1;
            }
        }
        return -1;
}



public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        int i=0;
        for(auto x:numbers){
            int raim=target-x;
            int searchResult=bsearch(numbers, raim,i+1);
            if(searchResult!=-1){
                return {i+1, searchResult+1}; // 1 based indx
            }
            i++;
        }
        return{};// as exactly one soln..
    }
};
```

## Test Results

- **Correct:** 24
- **Total testcases:** 24

