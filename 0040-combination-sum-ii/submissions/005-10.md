# Attempt 5

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2058461893 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 1 ms ms |
| Runtime Display | 1 ms |
| Runtime Percentile | 49.3569% |
| Memory | 14.4 MB |
| Memory Display | 14.4 MB |
| Memory Percentile | 15.89919999999998% |
| Submitted | 2026-07-06T18:44:55.000Z |

---

## Code

```cpp
class Solution {
    set<vector<int>> st;
    vector<int>subset;

    void dfs(vector<int>&arr, int idx, int target){

        if(target==0){
            st.insert(subset);
            return;
        }

        if(idx==arr.size() || target<0){
            return;
        }

        for(int i=idx; i<arr.size(); i++){
            if(i>idx && arr[i]==arr[i-1]){
                continue;
            }

            if(arr[i]>target){
                break;
            }
    
            subset.push_back(arr[i]);
            dfs(arr, i+1, target-arr[i]); //include
            subset.pop_back();
        }
        

    }


public:
    vector<vector<int>> combinationSum2(vector<int>& candidates, int target) {
        sort(candidates.begin(), candidates.end());
        dfs(candidates, 0, target);
        return (vector<vector<int>>){st.begin(), st.end()};
    }
};
```

## Test Results

- **Correct:** 176
- **Total testcases:** 176

