# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2104292216 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 14.4 MB |
| Memory Display | 14.4 MB |
| Memory Percentile | 15.89919999999998% |
| Submitted | 2026-08-12T14:24:10.000Z |

---

## Code

```cpp
class Solution {
set<vector<int>>st;

void solve(vector<int>& candidates, int target, vector<int>&curr, int idx){

    if(target==0){
        st.insert(curr);
        return;
    }
    if(idx>=candidates.size() ||target<0){
        return;
    }

    for (int i = idx; i < candidates.size(); i++) {

            // Skip duplicates at the same recursion level
            if (i > idx && candidates[i] == candidates[i - 1])
                continue;

            // Since sorted, no later value can work
            if (candidates[i] > target)
                break;

            // TAKE
            curr.push_back(candidates[i]);

            solve(candidates,
                  target - candidates[i],
                  curr,
                  i + 1
                  );

            // undo
            curr.pop_back();
    }
    
}



public:
    vector<vector<int>> combinationSum2(vector<int>& candidates, int target) {
        vector<int>curr;
        sort(candidates.begin(), candidates.end()); // sorting preser  // nlogn
        solve(candidates, target, curr, 0);
        vector<vector<int>> ans= {st.begin(), st.end()};
        return ans;
    }
};
```

## Test Results

- **Correct:** 176
- **Total testcases:** 176

