# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2058462398 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 4 ms ms |
| Runtime Display | 4 ms |
| Runtime Percentile | 21.603800000000003% |
| Memory | 14.1 MB |
| Memory Display | 14.1 MB |
| Memory Percentile | 39.58709999999998% |
| Submitted | 2026-07-06T18:45:26.000Z |

---

## Code

```cpp
class Solution {
    vector<vector<int>> st;
    vector<int>subset;

    void dfs(vector<int>&arr, int idx, int target){

        if(target==0){
            st.push_back(subset);
            return;
        }

        if(idx==arr.size() || target<0){
            return;
        }

        for(int i=idx; i<arr.size(); i++){
            if(i>idx && arr[i]==arr[i-1]){
                continue;
            }

            if(arr[i]>target){
                break;
            }
    
            subset.push_back(arr[i]);
            dfs(arr, i+1, target-arr[i]); //include
            subset.pop_back();
        }
        

    }


public:
    vector<vector<int>> combinationSum2(vector<int>& candidates, int target) {
        sort(candidates.begin(), candidates.end());
        dfs(candidates, 0, target);
        return st;
    }
};
```

## Test Results

- **Correct:** 176
- **Total testcases:** 176

