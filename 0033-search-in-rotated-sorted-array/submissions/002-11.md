# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2035696025 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-06-16T23:34:10.000Z |

---

## Code

```cpp
class Solution {
public:
    int search(vector<int>& arr, int target) {
      int left=0, right=arr.size()-1;

      while(left<=right){
        int mid=(left+right)/2;

        if(arr[mid]== target){
            return mid;
        }else if(arr[left] <= arr[mid]){ // left half sorted..
            if(target>=arr[left] && target<arr[mid]){
                right=mid-1;
            }else{
                left=mid+1;
            }

        }else{ // right half sorted..
            if(target>arr[mid] && target<arr[right]){
                left=mid+1;
            }else{
                right=mid-1;
            }
        }
      }  
      return -1;
    }
};
```

## Test Results

- **Correct:** 187
- **Total testcases:** 196

## Last Testcase

```text
[5,1,3]
3
```

## Code Output

```text
-1
```

## Expected Output

```text
2
```

