# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2117729021 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 29.2 MB |
| Memory Display | 29.2 MB |
| Memory Percentile | 11.03749999999999% |
| Submitted | 2026-08-23T18:42:19.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {


bool dfs(TreeNode *a, TreeNode *b){

    if(!a && !b){
        return true;
    }
    if(!a || !b){
        return false;// any one end before others ...
    }
    if(a->val!=b->val){
        return false;
    }

    bool c1=dfs(a->left, b->left);
    bool c2= dfs(a->right, b->right);
    return c1 && c2; // both shold return true else false..
}


// not bst..

vector<TreeNode*> tt;

void serc(TreeNode* root , int val){
    if(!root){
        return;
    }
    if(root->val ==val){
        tt.push_back(root);
    }
    serc(root->left , val);
    serc(root->right, val);
}



public:
    bool isSubtree(TreeNode* root, TreeNode* subRoot) {
        
        serc(root, subRoot->val);

        for(auto x:tt){
            if(dfs(x, subRoot)){
                return true;
            }
        }
        return false;

    }
};
```

## Test Results

- **Correct:** 185
- **Total testcases:** 185

