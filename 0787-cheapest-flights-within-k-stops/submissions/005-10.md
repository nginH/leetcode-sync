# Attempt 5

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2091307328 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 1 ms ms |
| Runtime Display | 1 ms |
| Runtime Percentile | 73.3082% |
| Memory | 17.8 MB |
| Memory Display | 17.8 MB |
| Memory Percentile | 65.97789999999998% |
| Submitted | 2026-08-02T11:56:19.000Z |

---

## Code

```cpp
class Solution {
public:
    int findCheapestPrice(int n, vector<vector<int>>& flights,
                          int src, int dst, int k) {
        vector<vector<pair<int,int>>> adj(n);
        for (auto &x : flights) {
            adj[x[0]].push_back({x[1], x[2]});
        }

        vector<int> price(n,INT_MAX);
        price[src]=0;
        queue<pair<int, pair<int, int>>> q; // stop ,node, cost;
        q.push({0, {src , 0}});
        
        
        while(!q.empty()){
            auto [stop, stage]=q.front();
            auto [node , cost]=stage;
            q.pop();
            if(stop>k){
                continue;
            }
           for(auto [next, wt]:adj[node]){
                int newCost= wt+cost;
                if(newCost<price[next]){
                    price[next]= newCost;
                    q.push({stop+1,{next, price[next]}});
                }
            }
        }




        return price[dst]==INT_MAX?-1:price[dst];
    }
};
```

## Test Results

- **Correct:** 59
- **Total testcases:** 59

