# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2102781363 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 16.4 MB |
| Memory Display | 16.4 MB |
| Memory Percentile | 15.995399999999965% |
| Submitted | 2026-08-11T12:01:03.000Z |

---

## Code

```cpp
class Solution {
private:
int m;
int n;
    vector<pair<int, int>> dir{
        {1, 0},
        {0, 1}
    };
    int sol(int row, int col, vector<vector<int>>& grid, vector<vector<int>>&dp){
        if (row >= m || col >= n) {
            return INT_MAX;
        }

        if (row == m-1 && col == n-1) {
           return grid[row][col];
        }
        if(dp[row][col]!=-1){
            return dp[row][col];
        }

        int curr=grid[row][col];

        int upRes=sol(row+1, col, grid, dp);
        int dowRes=sol(row, col+1, grid, dp);

       return dp[row][col]= curr+ min(upRes,dowRes);
    }


public:
    int minPathSum(vector<vector<int>>& grid) {
         m= grid.size();
         n=grid[0].size();
         vector<vector<int>>dp(m+1, vector<int>(n+1, -1));
        return sol(0,0,grid, dp);
    }
};
```

## Test Results

- **Correct:** 66
- **Total testcases:** 66

