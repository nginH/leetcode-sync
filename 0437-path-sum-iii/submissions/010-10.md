# Attempt 10

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2059911538 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 11 ms ms |
| Runtime Display | 11 ms |
| Runtime Percentile | 39.0371% |
| Memory | 21.7 MB |
| Memory Display | 21.7 MB |
| Memory Percentile | 24.25609999999999% |
| Submitted | 2026-07-08T02:59:16.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

private:

 int ans=0;
unordered_map<long long, int> mp;
 void dfs(TreeNode* root, long long target, long long sum) {
        if (!root)
            return;
        sum += root->val;
        ans += mp[sum - target];
        mp[sum]++;
        dfs(root->left, target, sum);
        dfs(root->right, target, sum);
        mp[sum]--;


    }

public:
    int pathSum(TreeNode* root, int k) {
        if(root==nullptr){
            return 0;
        }
        mp[0]=1;
        dfs(root, k, 0);
        return ans;
    }
};
```

## Test Results

- **Correct:** 130
- **Total testcases:** 130

