# Attempt 13

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2059903877 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-07-08T02:43:36.000Z |

---

## Code

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {

private:

int  sumk(TreeNode *root, int sum ){
    if(!root){
        return 0;
    }
    int res=0;
    if(sum==root->val){
        res+=1;
    }

    res+= sumk(root->left,sum-root->val );
    res+= sumk(root->right,sum-root->val );
    return res;
}


public:
    int pathSum(TreeNode* root, int k) {
        if(!root){
            return 0;
        }
        return sumk(root->left,k)+sumk(root, k)+ sumk(root->right,k);
    }
};
```

## Test Results

- **Correct:** 103
- **Total testcases:** 130

## Last Testcase

```text
[1,null,2,null,3,null,4,null,5]
3
```

## Code Output

```text
1
```

## Expected Output

```text
2
```

