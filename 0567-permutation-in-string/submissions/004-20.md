# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2096542660 |
| Status | 20 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-06T10:39:24.000Z |

---

## Code

```cpp
class Solution {
public:
    bool checkInclusion(string s1, string s2) {

            if(s1.size()>s2.size()){
                return false;
            }
        
            unordered_map<char, int>s1mp;
            for(auto x:s1){
                s1mp[x]++;
            }


            for(int right=0;right<s2.size();right++){
                if(s1mp.contains(s2[right])){
                    unordered_map<char,int> temp=s1mp;
                    int left=right;
                    int sz=s1.size();
                    while(sz--){
                        if(!s1mp.contains(s2[left])){
                            break;
                        }
                        s1mp[s2[left]]--; // value not exist it'll add neg value increment
                        if(s1mp[s2[left]]==0){
                            s1mp.erase(s2[left]);
                        }
                        left++;
                    }
                    if(mp.size()==0){
                        return true;
                    }
                }
            }

        
            return false;

    }
};
```

## Test Results

- **Total testcases:** 0

## Compile Error

```text
Line 30: Char 24: error: use of undeclared identifier 'mp'
   30 |                     if(mp.size()==0){
      |                        ^
1 error generated.
```

## Code Output

```text
{"compile_error": "Line 30: Char 24: error: use of undeclared identifier 'mp'\n   30 |                     if(mp.size()==0){\n      |                        ^\n1 error generated."}
```

