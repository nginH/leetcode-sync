# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2121071722 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 32 ms ms |
| Runtime Display | 32 ms |
| Runtime Percentile | 21.819599999999973% |
| Memory | 85.7 MB |
| Memory Display | 85.7 MB |
| Memory Percentile | 43.48920000000001% |
| Submitted | 2026-08-26T17:05:11.000Z |

---

## Code

```cpp
class Solution {

    vector<int> prevSmaller(vector<int>& heights) {
        stack<int> st;
        int n = heights.size();
        vector<int> res(n, -1);

        for (int i = 0; i < n; i++) {
            while (!st.empty() && heights[st.top()] >= heights[i]) {
                st.pop();
            }
            if (!st.empty()) {
                res[i] = st.top();
            }
            st.push(i);
        }

        return res;
    }

    vector<int> Smaller(vector<int>& heights) {
        stack<int> st;
        int n = heights.size();
        vector<int> res(n, n);
        for (int i = n - 1; i >= 0; i--) {
            while (!st.empty() && heights[st.top()] >= heights[i]) {
                st.pop();
            }
            if (!st.empty()) {
                res[i] = st.top();
            }
            st.push(i);
        }
        return res;
    }

public:
    int largestRectangleArea(vector<int>& heights) {

        vector<int> nextSM = Smaller(heights);
        vector<int> prevSM = prevSmaller(heights);

        int mx = 0;
        for (int i = 0; i < heights.size(); i++) {
            int width = nextSM[i] - prevSM[i] - 1;
            mx = max(mx, heights[i] * width);
        }

        return mx;
    }
};
```

## Test Results

- **Correct:** 99
- **Total testcases:** 99

