# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2102810913 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 572 ms ms |
| Runtime Display | 572 ms |
| Runtime Percentile | 13.507400000000098% |
| Memory | 296.8 MB |
| Memory Display | 296.8 MB |
| Memory Percentile | 6.9309000000000065% |
| Submitted | 2026-08-11T12:31:49.000Z |

---

## Code

```cpp
class Solution {


int sol(vector<int>&nums, int idx, int lastIdx, vector<vector<int>>&dp){

    if(idx>=nums.size()){
        return 0;
    }
   
    if (dp[idx][lastIdx + 1] != -1) {
        return dp[idx][lastIdx + 1];
    }
    int take=0;
    int skip=sol(nums, idx+1, lastIdx, dp);
     if (lastIdx == -1 || nums[idx] > nums[lastIdx]) { // bada hai tabli allowd ..
        take=1+sol(nums, idx+1,idx,  dp);
    }
     return dp[idx][lastIdx + 1] = max(take, skip);


    
}


public:
    int lengthOfLIS(vector<int>& nums) {
        vector<vector<int>>dp(nums.size()+1, vector<int>(nums.size()+1,  -1));
        return sol(nums, 0, -1, dp);
    }
};
```

## Test Results

- **Correct:** 56
- **Total testcases:** 56

