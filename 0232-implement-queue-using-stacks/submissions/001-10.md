# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2103485932 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 0 ms ms |
| Runtime Display | 0 ms |
| Runtime Percentile | 100% |
| Memory | 10.1 MB |
| Memory Display | 10.1 MB |
| Memory Percentile | 47.21830000000002% |
| Submitted | 2026-08-11T23:16:45.000Z |

---

## Code

```cpp
class MyQueue {
public:

    stack<int>input;
    stack<int>output;
    MyQueue() {
        
    }
    
    void push(int x) {
        this->input.push(x);
    }
    
    int pop() {
        if(!this->output.empty()){
            int temp= this->output.top();
            this->output.pop();
            return temp;
        }else if(this->output.empty() && !this->input.empty()){
            while(!this->input.empty()){
                this->output.push(this->input.top());
                this->input.pop();
            }
            int temp= this->output.top();
            output.pop();
            return temp;

        }else{
            return -1;
        }
    }
    
    int peek() {
        if(!this->output.empty()){
            return output.top();
        }else if(output.empty() && !input.empty()){
            while(!this->input.empty()){
                this->output.push(this->input.top());
                this->input.pop();
            }
            return this->output.top();
        }else{
            return -1;
        }
    }
    
   bool empty() {
    if(!this->output.empty()){
        return false;
    }
    if(!this->input.empty()){
        return false;
    }

    return true;
}
};

/**
 * Your MyQueue object will be instantiated and called as such:
 * MyQueue* obj = new MyQueue();
 * obj->push(x);
 * int param_2 = obj->pop();
 * int param_3 = obj->peek();
 * bool param_4 = obj->empty();
 */
```

## Test Results

- **Correct:** 23
- **Total testcases:** 23

