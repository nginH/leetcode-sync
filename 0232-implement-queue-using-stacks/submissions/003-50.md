# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2103484571 |
| Status | 50 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-08-11T23:12:05.000Z |

---

## Code

```cpp
class MyQueue {
public:

    stack<int>input;
    stack<int>output;
    MyQueue() {
        
    }
    
    void push(int x) {
        this->input.push(x);
    }
    
    int pop() {
        if(!this->output.empty()){
            return this->output.top();
        }else if(this->output.empty() && !this->input.empty()){
            while(!this->input.empty()){
                this->output.push(this->input.top());
                this->input.pop();
            }
            int temp= this->output.top();
            output.pop();
            return temp;

        }else{
            return -1;
        }
    }
    
    int peek() {
        if(!this->output.empty()){
            return output.top();
        }else if(output.empty() && !input.empty()){
            while(!this->input.empty()){
                this->output.push(this->input.top());
                this->input.pop();
            }
            return this->output.top();
        }else{
            return -1;
        }
    }
    
    bool empty() {
        if(this->output.empty()){
            return false;
        }
        if(this->input.empty()){
            return false;
        }

        return true;
    }
};

/**
 * Your MyQueue object will be instantiated and called as such:
 * MyQueue* obj = new MyQueue();
 * obj->push(x);
 * int param_2 = obj->pop();
 * int param_3 = obj->peek();
 * bool param_4 = obj->empty();
 */
```

## Test Results

- **Correct:** 12
- **Total testcases:** 23

## Last Testcase

```text
["MyQueue","empty"]
[[],[]]
```

## Code Output

```text
[null,false]
```

## Expected Output

```text
[null,true]
```

