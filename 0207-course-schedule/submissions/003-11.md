# Attempt 3

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2139986863 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-09-12T20:20:44.000Z |

---

## Code

```cpp
class Solution {

    bool canF(int numCourses, vector<vector<int>>& prerequisites) {
        vector<int> indegree(numCourses);
        vector<vector<int>> adj(numCourses);
        for (auto x : prerequisites) {
            indegree[x[0]]++;
            adj[x[1]].push_back(x[0]);
        }

        queue<int> q;

        for (int i = 0; i < indegree.size(); i++) {
            if (indegree[i] == 0) {
                q.push(i);
            }
        }
        int level = 0;
        while (!q.empty()) {
            int sz = q.size();
            level++;
            while (sz--) {
                int k = q.front();
                q.pop();
                for (auto x : adj[k]) {
                    indegree[x]--;
                    if (indegree[x] == 0) {
                        q.push(x);
                    }
                }
            }
        }

        return level == numCourses;
    }

public:
    bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        return canF(numCourses, prerequisites);
    }
};
```

## Test Results

- **Correct:** 39
- **Total testcases:** 54

## Last Testcase

```text
2
[]
```

## Code Output

```text
false
```

## Expected Output

```text
true
```

