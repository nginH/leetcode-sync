# Attempt 1

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2105926763 |
| Status | 10 |
| Language | cpp |
| Language Version | C++ |
| Runtime | 4 ms ms |
| Runtime Display | 4 ms |
| Runtime Percentile | 64.7153% |
| Memory | 20 MB |
| Memory Display | 20 MB |
| Memory Percentile | 40.74549999999996% |
| Submitted | 2026-08-13T19:58:54.000Z |

---

## Code

```cpp
class Solution {
public:
    bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        int n=numCourses;
        vector<vector<int>> adj(n);
        vector<int>indegree(n);
        for(auto x:prerequisites){
            adj[x[1]].push_back(x[0]);
            indegree[x[0]]++;
        }
        queue<int>q;
        for(int i=0; i<indegree.size(); i++){
            if(indegree[i]==0){
              q.push(i);  
            }
        }

        int processNode=0;
        while(!q.empty()){
            int currNode= q.front();
            q.pop();
            processNode++;
            for(auto x:adj[currNode]){
                indegree[x]--;
                if(indegree[x]==0){
                    q.push(x);
                }
            }
        }
        return numCourses==processNode;


    }
};
```

## Test Results

- **Correct:** 54
- **Total testcases:** 54

