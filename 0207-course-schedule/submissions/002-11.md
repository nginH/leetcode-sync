# Attempt 2

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2139987836 |
| Status | 11 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-09-12T20:22:35.000Z |

---

## Code

```cpp
class Solution {

    bool canF(int numCourses, vector<vector<int>>& prerequisites) {
        vector<int> indegree(numCourses);
        vector<vector<int>> adj(numCourses) ; for (auto x : prerequisites) {
            indegree[x[0]]++;
            adj[x[1]].push_back(x[0]);
        }

        queue<int> q;

        for (int i = 0; i < indegree.size(); i++) {
            if (indegree[i] == 1) {
                q.push(i);
            }
        }
        int level = 0;
        while (!q.empty()) {

            level++;

            int k = q.front();
            q.pop();
            for (auto x : adj[k]) {
                indegree[x]--;
                if (indegree[x] == 1) {
                    q.push(x);
                }
            }
        }

        return level != numCourses;
    }

public:
    bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        return canF(numCourses, prerequisites);
    }
};
```

## Test Results

- **Correct:** 36
- **Total testcases:** 54

## Last Testcase

```text
20
[[0,10],[3,18],[5,5],[6,11],[11,14],[13,1],[15,1],[17,4]]
```

## Code Output

```text
true
```

## Expected Output

```text
false
```

