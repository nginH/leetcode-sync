# Attempt 4

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2036982719 |
| Status | 15 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-06-18T03:30:13.000Z |

---

## Code

```cpp
class Solution {
public:


    int findPeakElement(vector<int>& arr) {
        int low=0, high=arr.size()-1, lb=arr.size()+1;
        while(low<=high){
            int mid=(low+high)/2;
            if(mid>0 && mid<arr.size()-1){

                if(arr[mid]>arr[mid-1] && arr[mid]>arr[mid+1]){
                    return mid;
                }

                if(arr[mid]>arr[mid-1] && arr[mid]<arr[mid+1]){ //increaseing 
                    // so peak should at right 
                    low=mid+1;
                }else{// decreasing 
                    high=mid-1;
                }
            }else{
                low=mid+1; // can both increae lower bound or upper bound
            }
        }

        return abs(arr[0])>abs(arr[arr.size()-1])?0:arr.size()-1;


    }
};
```

## Test Results

- **Correct:** 4
- **Total testcases:** 73

## Runtime Error

```text
Line 26: Char 20: runtime error: negation of -2147483648 cannot be represented in type 'int'; cast to an unsigned type to negate this value to itself (solution.cpp)
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior prog_joined.cpp:35:20
```

## Last Testcase

```text
[-2147483648]
```

## Expected Output

```text
0
```

