# Attempt 9

## Submission Metadata

| Field | Value |
|---|---|
| Submission ID | 2036978076 |
| Status | 14 |
| Language | cpp |
| Language Version | C++ |
| Runtime | N/A ms |
| Runtime Display | N/A |
| Memory | N/A |
| Memory Display | N/A |
| Submitted | 2026-06-18T03:22:21.000Z |

---

## Code

```cpp
class Solution {
public:


    int findPeakElement(vector<int>& arr) {
        int low=0, high=arr.size()-1, lb=arr.size()+1;


        if(arr.size()==0){
            return 0;
        }
        while(low<=high){
            int mid=(low+high)/2;
            if(mid>0 && mid<arr.size()-1){

                if(arr[mid]>arr[mid-1] && arr[mid]>arr[mid+1]){
                    return mid;
                }

                if(arr[mid]>arr[mid-1] && arr[mid]<arr[mid+1]){ //increaseing 
                    // so peak should at right 
                    low=mid+1;
                }else{// decreasing 
                    high=mid-1;
                }
            }   
            
        }

        return max(arr[0], arr[arr.size()-1]);


    }
};
```

## Test Results

- **Correct:** 2
- **Total testcases:** 73

## Last Testcase

```text
[1]
```

## Expected Output

```text
0
```

